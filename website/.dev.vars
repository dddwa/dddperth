SESSION_SECRET=local-dev-secret
WEB_URL=http://localhost:3800

# Private Sessionize endpoints for the current year. The bare API URL exposes
# unpublished sessions, so it must not be committed. Fill these in locally with
# the values from 1Password / the Sessionize event admin page.
SESSIONIZE_2026_SESSIONS=
SESSIONIZE_2026_ALL_SESSIONS=
